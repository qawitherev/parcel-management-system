import { Routes } from '@angular/router';
import { NotificationPrefs } from './notification-prefs';

export const NOTIFICATION_PREFS_ROUTES: Routes = [
  { path: '', component: NotificationPrefs }
];
