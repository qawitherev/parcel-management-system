name: CD — Production

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/cd.yml
    with:
      environment: production
    permissions:
      id-token: write
      contents: read
